# codecov.yml
coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 2%
    patch:
      default:
        target: 80%
        threshold: 2%

ignore:
  - "backend/test/**/*"
  - "**/site-packages/**/*"
  - "**/__init__.py"